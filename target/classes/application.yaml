spring:
  application:
    name: saferent-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/saferent_db
    username: postgres
    password: sara1234
  jpa:
    hibernate:
      ddl-auto: update    # используем миграции
    show-sql: true
#  flyway:                     # или liquibase, если предпочитаешь
#    enabled: true
#    locations: classpath:db/migration
#
#jwt:
#  secret: your_very_long_secret_key_here_32+chars
#  expiration: 86400000   # 24 часа в мс
#
#logging:
#  level:
#    org.springframework: INFO
#    kz.saferent: DEBUG

jwt:
  secret: saferent_super_secret_key_must_be_32_chars_min
  expiration: 86400000  # 24 часа в мс

ai:
  service:
    url: http://localhost:8000
